# ──────────────────────────────────────────────────────────────
# DockerForge environment configuration
# Copy this file to `.env` and fill in real values:  cp .env.example .env
# `.env` is git-ignored — never commit real secrets.
# ──────────────────────────────────────────────────────────────

# --- LLM provider (Google Gemini) ---
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash

# --- Backend server ---
# Comma-separated list of origins allowed to call the API (the frontend dev URL).
CORS_ORIGINS=http://localhost:5173

# --- Agent guardrails (sane defaults; tune in later phases) ---
CLONE_TIMEOUT_SECONDS=60
MAX_REPO_MB=200
BUILD_TIMEOUT_SECONDS=600
RUN_TIMEOUT_SECONDS=60
MAX_BUILD_ATTEMPTS=3

# --- Frontend (Vite) ---
# Where the browser should reach the backend API.
VITE_API_BASE_URL=http://localhost:8000
