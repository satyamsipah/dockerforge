# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / settings ---
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- LLM provider (Phase 3) ---
# google-genai: used when GEMINI_BASE_URL is not set (direct Gemini API)
# openai: used when GEMINI_BASE_URL is set (OpenRouter / any OAI-compat proxy)
google-genai>=1.5.0
openai>=1.0.0

# --- HTTP client for health-check polling (Phase 5) ---
httpx>=0.27.0

# SSE is implemented via Starlette's StreamingResponse (no extra dep needed)

# --- Dev / test ---
pytest>=8.0

# Later phases add: docker, sse-starlette
